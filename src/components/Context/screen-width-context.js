import React from 'react';

/**
 * this context has the screen width value, but i didnt use it, i just made it to practice.
 */
const ScreenWidth = React.createContext({
	screenWidth: 0,
});

export default ScreenWidth;
